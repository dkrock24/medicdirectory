<nav class="navbar navbar-inverse">
    <div class="container-fluid">
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li {% if ( app.request.uri == url('support_homepage_crear') ) %}class="active"{% endif %}><a href="{{ path('support_homepage_crear') }}">Solicitar soporte</a></li>
                <li {% if ( app.request.uri == url('support_homepage_listar') ) %}class="active"{% endif %}><a href="{{ path('support_homepage_listar') }}">Ver mis tickets</a></li>
            </ul>
            <form class="navbar-form navbar-left">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder="Caso de soporte">
                </div>
                <button type="submit" class="btn btn-default">Búscar</button>
            </form>

        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>
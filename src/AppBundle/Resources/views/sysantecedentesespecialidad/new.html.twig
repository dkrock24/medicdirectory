{% extends '_admin_base.html.twig' %}

{% block breadcrumb %}
    {{ include('AppBundle:sysantecedentesespecialidad:_breadcrumb.html.twig' )  }}
{% endblock %} 

{% block submenutop %}
    {{ include('AppBundle:sysantecedentesespecialidad:_submenu.html.twig' )  }}
{% endblock %} 

{% block body %}
    <h1>Nueva entrada</h1>

    <div class="row">
        <div class="col-lg-6 col-md-6 col-sm-12">

        {# {{ dump( form_widget(form) )}} #}
            <div class="panel-bodyx">

            {{ form_start(form) }}
                {# {{ form_widget(form) }} #}

                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.antecedente)}}</div>
                    <div><label for="">Antecedente</label></div>
                    {{ form_widget(form.antecedente) }}
                </div>

                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.valor)}}</div>
                    <div><label for="">Valor</label></div>
                    {{ form_widget(form.valor) }}
                </div>

                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.descripcion)}}</div>
                    <div><label for="">Descripcion</label></div>
                    {{ form_widget(form.descripcion) }}
                </div>

                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.IdEspecialidad)}}</div>
                    <div><label for="">Id Especialidad</label></div>
                    {{ form_widget(form.IdEspecialidad) }}
                </div>


                <input type="submit" value="Crear" class="btn btn-primary" />
            {{ form_end(form) }}

            {#
            <ul>
                <li>
                    <a href="{{ path('sysantecedentesespecialidad_index') }}">Back to the list</a>
                </li>
            </ul>
            #}
            </div>  
    </div>
{% endblock %}

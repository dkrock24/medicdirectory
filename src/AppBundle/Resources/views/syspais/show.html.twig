{% extends "::base.html.twig" %}
  
{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>SysPais</h1>

            <table>
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ sysPai.id }}</td>
                    </tr>
                    <tr>
                        <th>Nombre</th>
                        <td>{{ sysPai.nombreSysPais }}</td>
                    </tr>
                    <tr>
                        <th>Prefijo</th>
                        <td>{{ sysPai.prefijoSysPais }}</td>
                    </tr>
                    <tr>
                        <th>Código postal</th>
                        <td>{{ sysPai.codigoPostalSysPais }}</td>
                    </tr>
                    <tr>
                        <th>Continente</th>
                        <td>{{ sysPai.contientePaisSysPais }}</td>
                    </tr>
                    <tr>
                        <th>Estatus</th>
                        <td>{{ sysPai.estadoSysPais }}</td>
                    </tr>
                </tbody>
            </table>

            <ul>
                <li>
                    <a href="{{ path('syspais_index') }}">Regresar a la lista</a>
                </li>
                <li>
                    <a href="{{ path('syspais_edit', { 'id': sysPai.id }) }}">Editar</a>
                </li>
                <li>
                    {{ form_start(delete_form) }}
                    <input type="submit" value="Delete">
                    {{ form_end(delete_form) }}
                </li>
            </ul>
        
        </div>
    </div>
{% endblock %}

{% block codejs %}
    <script>
        $(function(){

          });
    </script>
{% endblock %}  

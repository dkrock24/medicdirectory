{% extends '_admin_base.html.twig' %}

{% block breadcrumb %}
    {{ include('AppBundle:sysexamenesmedicos:_breadcrumb.html.twig' )  }}
{% endblock %} 

{% block submenutop %}
    {{ include('AppBundle:sysexamenesmedicos:_submenu.html.twig' )  }}
{% endblock %} 

{% block body %}
    <h1>Crear Examenes Medicos</h1>
    <div class="row">
        <div class="col-lg-6 col-md-6 col-sm-12">

    {{ form_start(form) }}
         {# {{ dump( form_widget(form) )}} #}
         <div class="panel-bodyx">                
            
                {# {{ form_widget(form) }} #}
                <div class="form_error">{{ form_errors(form)}}</div>

                <div class="form-group">                    
                    <div class="form_error">{{ form_errors(form.nombreExamen)}}</div>
                    <div><label for="">Nombre</label></div>
                    {{ form_widget(form.nombreExamen) }}
                </div>

                <div class="form-group">                    
                    <div class="form_error">{{ form_errors(form.tipoExamen)}}</div>
                    <div><label for="">Tipo</label></div>
                    {{ form_widget(form.tipoExamen) }}
                </div>

                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.estadoExamen)}}</div>
                    <div>Estado</div>
                    <div class="checkbox">
                            <label>
                                {{ form_widget(form.estadoExamen) }}
                                <span class="text"></span>
                            </label>
                    </div>   
                </div>
            <input type="submit" value="Crear" class="btn btn-primary" />
        {{ form_end(form) }}
    </div>                
    </div>
    {#
    <ul>
        <li>
            <a href="{{ path('sysexamenesmedicos_index') }}">Back to the list</a>
        </li>
    </ul>
     #}
{% endblock %}

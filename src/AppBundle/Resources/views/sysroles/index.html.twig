{% extends "::_admin_base.html.twig" %}

{% block breadcrumb %}
    {{ include('AppBundle:sysroles:_breadcrumb.html.twig' )  }}
{% endblock %} 

{% block submenutop %}
    {{ include('AppBundle:sysroles:_submenu.html.twig' )  }}
{% endblock %} 

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>Lista de Roles</h1>

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nombrerol</th>
                        <th>Esactivo</th>
                        <th>Fecha creación</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for sysRole in sysRoles %}
                        <tr>
                            <td><a href="{{ path('sysroles_show', { 'id': sysRole.id }) }}">{{ sysRole.id }}</a></td>
                            <td>{{ sysRole.nombreRolSysRoles }}</td>
                            <td>{% if sysRole.estatusSysRoles %}<span class="label label-success">Activo</span>{% else %}<span class="label label-danger">Inactivo</span>{% endif %}</td>
                            <td>{{ sysRole.fechaCreacionSysRoles|date('Y-m-d H:i:s') }}</td>
                            <td>
                                <ul class="btn-option-emr">
                                    <li>
                                        <a href="{{ path('sysroles_show', { 'id': sysRole.id }) }}" class="btn btn-default"><i class="glyphicon glyphicon-eye-open"></i> Ver</a>
                                    </li>
                                    <li>
                                        <a href="{{ path('sysroles_edit', { 'id': sysRole.id }) }}" class="btn btn-info"><i class="glyphicon glyphicon-pencil"></i> Editar</a>
                                    </li>
                                </ul>    
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
           {#     
            <ul>
                <li>
                    <a href="{{ path('sysroles_new') }}">Create a new entry</a>
                </li>
            </ul>
           #}
        </div>
    </div>
{% endblock %}

{% block codejs %}
    <script>
        $(function(){

          });
    </script>
{% endblock %} 
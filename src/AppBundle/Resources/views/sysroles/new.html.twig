{% extends "::base.html.twig" %}
 
{% block breadcrumb %}
    {{ include('AppBundle:sysroles:_breadcrumb.html.twig' )  }}
{% endblock %} 

{% block submenutop %}
    {{ include('AppBundle:sysroles:_submenu.html.twig' )  }}
{% endblock %} 

{% block body %}
    <div class="row">
        <div class="col-lg-6 col-md-6 col-sm-12">
            <h1>Nuevo Role</h1>

            {{ form_start(form, {'attr': {'class': ''}}) }}    
            
                {# {{ form_widget(form) }} #}
                <div class="form_error">{{ form_errors(form)}}</div>
                <div class="form-group">
                    
                    <div class="form_error">{{ form_errors(form.nombreRolSysRoles)}}</div>
                    <div><label for="">Nombre</label></div>
                    {{ form_widget(form.nombreRolSysRoles) }}
                </div>
                <div class="form-group">
                    <div class="form_error">{{ form_errors(form.estatusSysRoles)}}</div>
                    <div>Estatus</div>
                    <div class="checkbox">
                            <label>
                                {{ form_widget(form.estatusSysRoles) }}
                                <span class="text"></span>
                            </label>
                    </div>   
                </div>
            
                                
                <input type="submit" value="Crear" class="btn btn-primary" />
            {{ form_end(form) }}
            {#
            <ul>
                <li>
                    <a href="{{ path('sysroles_index') }}">Back to the list</a>
                </li>
            </ul>
            #}    
        </div>
    </div>
{% endblock %}

{% block codejs %}
    <script>
        $(function(){

          });
    </script>
{% endblock %} 